apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: astronomy-shop-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.awsRegion | quote }}
